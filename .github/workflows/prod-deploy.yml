name: Deploy to AWS

on:
  push:
    branches:
      - main

jobs:
  deploy-dev:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 코드
        uses: actions/checkout@v4

      - name: Node.js 22 설정
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'yarn'
      - name: 🔹 .env 파일 생성
        run: |
          echo "${{ secrets.PROD_ENV }}" | tr ' ' '\n' > .env

      - name: 의존성 설치 및 빌드
        run: |
          yarn install
          yarn build
      - name: 파일 압축
        run: |
          zip -r build.zip .

      - name: S3에 업로드
        run: aws s3 cp build.zip s3://kickon-frontend-bucket/prod/latest-build.zip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-northeast-2

      - name: AWS CodeDeploy 실행
        run: |
          aws deploy create-deployment \
            --application-name kickon-prod-frontend \
            --deployment-group-name kickon-prod-frontend-group \
            --s3-location bucket=kickon-frontend-bucket,key=prod/latest-build.zip,bundleType=zip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-northeast-2
